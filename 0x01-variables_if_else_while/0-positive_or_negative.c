#include <time.h>
/**
 * main - generatr random numbers to dtect -ve or +ve
 *
 * Return: always (0)
 *
 */
int main(void)
{
	int n/

	strand(time(0));
	n= rand() - RAND_MAX / 2;
	if (n > 0)
	{
		printf("%d is positive\in", n);
	}
	else if (n == 0)
	{
		printf (n == 0)
	}
	else
	{
		printf("%d is ZERO\n", n);
	}
	else
	{
		printf("%d is negative\n", n);
	}
	return (0);
}
